!!!
%html
  %head
    %title= full_title(yield(:title))
    = stylesheet_link_tag "application", media: "all"
    = javascript_include_tag "application"
    = csrf_meta_tags

  %body
    .navbar
      .navbar-inner
        .container
          = link_to image_tag("logo.png", alt: "NYCorBUST"), root_path, class: "brand", height: "40px", width:"288px"
          %ul.nav.pull-right
            %li= link_to "All Items", items_path
            %li= link_to "By Category", categories_path
            %li= link_to "Contact", contact_path
            - if signed_in?
              %li= link_to "Admin", admin_path
              %li= link_to "Logout", logout_path
    .container
      .alert.alert-info.main-alert This site is not longer active. All NYCorBUST items have been sold. The site remains in place for display purposes only.
      = render 'shared/flash_messages'
      = yield

    = render 'layouts/analytics' unless Rails.env.development?
